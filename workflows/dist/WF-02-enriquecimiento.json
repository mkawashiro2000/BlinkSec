{
  "id": "blinksecwf02enrq",
  "name": "[BlinkSec] WF-02 - Enriquecimiento IoC - v1",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "wf02-trigger",
      "name": "Entrada desde WF-01",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.1,
      "position": [
        -600,
        300
      ],
      "notes": "inputSource=passthrough es OBLIGATORIO. Con una lista de campos vacía, el Execute Workflow Trigger no propaga NADA al subflujo: llega un objeto vacío y el flujo se ejecuta 'con éxito' sobre datos inexistentes. Encontrado en el primer despliegue real — WF-02 planificaba cero consultas de inteligencia."
    },
    {
      "parameters": {
        "jsCode": "// WF-02 · Preparación de consultas y lectura de caché.\n//\n// La caché de IoC no es una optimización opcional: con el free tier de\n// VirusTotal (4 peticiones/minuto), una ráfaga de alertas de la misma botnet\n// agota la cuota en segundos. Cachear por IoC —no por alerta— hace que la\n// segunda y sucesivas alertas de la misma campaña no consuman nada.\n//\n// Proveedores: AbuseIPDB, VirusTotal y CrowdSec CTI (para IPs). GreyNoise e\n// IBM X-Force se retiraron del sistema — nunca llegó a haber credenciales\n// operativas para ninguno de los dos (ver docs/riesgos.md).\n\n// ─── inyectado desde lib/enrich.js — NO EDITAR AQUÍ ───\n// Editar el fichero original y recompilar: npm run build\n/**\n * BlinkSec WF-02 — Enriquecimiento con inteligencia de amenazas.\n *\n * Las llamadas HTTP las hacen los nodos HTTP Request de n8n en paralelo; este\n * módulo contiene lo que sí es testeable en frío: el parseo de cada respuesta\n * a una forma común, la política de caché y el manejo de proveedores caídos.\n *\n * Decisión central: el enriquecimiento es DEGRADABLE. Con proveedores caídos\n * el sistema sigue produciendo un veredicto, pero lo marca como parcial —\n * y WF-03 impone un techo de puntuación para que un enriquecimiento\n * incompleto no pueda disparar contención automática. Es preferible escalar\n * a un humano de más que aislar un servidor porque VirusTotal devolvió 503.\n *\n * Proveedores: AbuseIPDB, VirusTotal y CrowdSec CTI. GreyNoise e IBM X-Force\n * se retiraron del sistema — nunca llegó a haber credenciales operativas\n * para ninguno de los dos (GreyNoise sin clave real conectada; X-Force con\n * un 401 que se confirmó como problema de la propia cuenta de IBM, no de\n * este sistema). CrowdSec CTI las sustituye como tercera fuente para IPs\n * (no resuelve hashes, igual que AbuseIPDB). Ver docs/riesgos.md.\n *\n * @injectable\n */\n\n/** TTL de caché en segundos, por tipo de veredicto. */\nconst CACHE_TTL = {\n  // Un IoC limpio puede ensuciarse: TTL corto.\n  clean: 3600,\n  // Un IoC malicioso rara vez se vuelve benigno: TTL largo, y es el caso que\n  // más consultas repetidas genera (misma botnet atacando en ráfaga).\n  malicious: 86400,\n  // Sin datos suficientes: reintentar pronto.\n  inconclusive: 900,\n  // Error del proveedor: no envenenar la caché con un fallo transitorio.\n  error: 60,\n};\n\nconst PROVIDERS = ['virustotal', 'abuseipdb', 'crowdsec'];\n\n// AbuseIPDB y VirusTotal son las fuentes \"core\": si una de ellas falla, el\n// enriquecimiento se marca parcial y WF-03 aplica el techo de seguridad.\n// CrowdSec es \"best-effort\": con sólo 120 consultas/mes (~4/día) en el plan\n// gratuito, cualquier ráfaga real de alertas agota la cuota en minutos. Si\n// contara como fuente core, un 429 de CrowdSec activaría el techo de\n// enriquecimiento parcial para TODAS las alertas del resto del día, aunque\n// AbuseIPDB y VirusTotal respondan con normalidad — el proveedor más débil\n// rompería la contención automática de todo el sistema. Cuando CrowdSec\n// responde, sigue sumando como corroboración extra en scoring/score.js; si\n// no responde, se ignora sin más, sin degradar nada.\nconst CORE_PROVIDERS = ['virustotal', 'abuseipdb'];\n\nfunction cacheKey(provider, iocType, value) {\n  return `blinksec:ioc:${provider}:${iocType}:${String(value).toLowerCase()}`;\n}\n\n/** Envoltura uniforme para cualquier respuesta de proveedor. */\nfunction wrap(provider, { available, verdict = 'inconclusive', data = {}, error = null }) {\n  return { provider, available, verdict, data, error };\n}\n\n// ---------------------------------------------------------------------------\n// VirusTotal — API v3\n// ---------------------------------------------------------------------------\n\nfunction parseVirusTotal(res, httpStatus = 200) {\n  // 404 = hash/IP desconocido para VT. Ausencia de evidencia, no evidencia de\n  // ausencia: un binario de malware a medida nunca estará en VT.\n  if (httpStatus === 404) {\n    return wrap('virustotal', { available: true, verdict: 'unknown', data: { known: false } });\n  }\n  // 429 = cuota agotada. El free tier son 4 req/min: en una ráfaga de alertas\n  // esto es lo normal, no lo excepcional. Por eso existe la caché.\n  if (httpStatus === 429) {\n    return wrap('virustotal', { available: false, error: 'rate_limited' });\n  }\n  if (httpStatus !== 200 || !res) {\n    return wrap('virustotal', { available: false, error: `HTTP ${httpStatus}` });\n  }\n\n  const stats = res.data?.attributes?.last_analysis_stats ?? {};\n  const malicious = Number(stats.malicious ?? 0);\n  const suspicious = Number(stats.suspicious ?? 0);\n  const harmless = Number(stats.harmless ?? 0);\n  const undetected = Number(stats.undetected ?? 0);\n  const total = malicious + suspicious + harmless + undetected;\n\n  const attrs = res.data?.attributes ?? {};\n  const data = {\n    known: true,\n    malicious,\n    suspicious,\n    total,\n    ratio: total > 0 ? malicious / total : 0,\n    // El nombre de familia que da el consenso, útil para el ticket.\n    popularName: attrs.popular_threat_classification?.suggested_threat_label ?? null,\n    reputation: attrs.reputation ?? null,\n  };\n\n  let verdict = 'inconclusive';\n  if (total === 0) verdict = 'unknown';\n  else if (malicious >= 5) verdict = 'malicious';\n  else if (malicious >= 1) verdict = 'suspicious';\n  else verdict = 'clean';\n\n  return wrap('virustotal', { available: true, verdict, data });\n}\n\n// ---------------------------------------------------------------------------\n// AbuseIPDB — API v2\n// ---------------------------------------------------------------------------\n\nfunction parseAbuseIPDB(res, httpStatus = 200) {\n  if (httpStatus === 429) {\n    return wrap('abuseipdb', { available: false, error: 'rate_limited' });\n  }\n  if (httpStatus !== 200 || !res?.data) {\n    return wrap('abuseipdb', { available: false, error: `HTTP ${httpStatus}` });\n  }\n\n  const d = res.data;\n  const score = Number(d.abuseConfidenceScore ?? 0);\n  const data = {\n    abuseConfidenceScore: score,\n    totalReports: Number(d.totalReports ?? 0),\n    distinctReporters: Number(d.numDistinctUsers ?? 0),\n    countryCode: d.countryCode ?? null,\n    isp: d.isp ?? null,\n    usageType: d.usageType ?? null,\n    lastReportedAt: d.lastReportedAt ?? null,\n    // Una IP dentro de la propia allowlist de AbuseIPDB del usuario.\n    isWhitelisted: d.isWhitelisted === true,\n  };\n\n  let verdict = 'clean';\n  if (d.isWhitelisted === true) verdict = 'benign';\n  else if (score >= 75) verdict = 'malicious';\n  else if (score >= 25) verdict = 'suspicious';\n  else if (score > 0) verdict = 'inconclusive';\n\n  return wrap('abuseipdb', { available: true, verdict, data });\n}\n\n// ---------------------------------------------------------------------------\n// CrowdSec CTI — /v2/smoke/{ip}\n// ---------------------------------------------------------------------------\n\nfunction parseCrowdSec(res, httpStatus = 200) {\n  // 404 = IP nunca reportada a la red de CrowdSec. Ausencia de evidencia, no\n  // evidencia de ausencia — igual que un hash desconocido para VirusTotal.\n  if (httpStatus === 404) {\n    return wrap('crowdsec', { available: true, verdict: 'unknown', data: { known: false } });\n  }\n  if (httpStatus === 429) {\n    return wrap('crowdsec', { available: false, error: 'rate_limited' });\n  }\n  if (httpStatus !== 200 || !res) {\n    return wrap('crowdsec', { available: false, error: `HTTP ${httpStatus}` });\n  }\n\n  // Escala 0-5 de CrowdSec CTI: cuánto de agresiva/amenazante es la IP según\n  // el consenso de la red de sensores CrowdSec.\n  const threat = Number(res.scores?.overall?.threat ?? 0);\n  const aggressiveness = Number(res.scores?.overall?.aggressiveness ?? 0);\n  const clasificaciones = (res.classifications?.classifications ?? []).map((c) => c.label ?? c.name).filter(Boolean);\n\n  const data = {\n    known: true,\n    threat,\n    aggressiveness,\n    classifications: clasificaciones,\n    backgroundNoise: res.background_noise_score != null ? Number(res.background_noise_score) : null,\n  };\n\n  let verdict = 'clean';\n  if (threat >= 3 || clasificaciones.length > 0) verdict = 'malicious';\n  else if (threat >= 1 || aggressiveness >= 1) verdict = 'suspicious';\n\n  return wrap('crowdsec', { available: true, verdict, data });\n}\n\n// ---------------------------------------------------------------------------\n// Agregación\n// ---------------------------------------------------------------------------\n\nconst PARSERS = {\n  virustotal: parseVirusTotal,\n  abuseipdb: parseAbuseIPDB,\n  crowdsec: parseCrowdSec,\n};\n\n/**\n * Consolida las respuestas de los nodos HTTP paralelos.\n *\n * Cada entrada es { provider, body, statusCode } tal y como la deja n8n con\n * \"Continue on Fail\" y \"Never Error\" activados — un proveedor caído llega\n * aquí como un statusCode != 200, no como una excepción que aborte el flujo.\n */\nfunction aggregate(responses) {\n  const enrichment = {};\n  for (const provider of PROVIDERS) enrichment[provider] = wrap(provider, { available: false, error: 'no_consultado' });\n\n  for (const r of responses ?? []) {\n    const parser = PARSERS[r.provider];\n    if (!parser) continue;\n    try {\n      enrichment[r.provider] = parser(r.body, r.statusCode ?? 200);\n    } catch (e) {\n      // Un cambio de esquema del proveedor no puede tumbar el SOAR entero.\n      enrichment[r.provider] = wrap(r.provider, { available: false, error: `parse_error: ${e.message}` });\n    }\n  }\n\n  // Se acota a proveedores CORE conocidos: una errata en el nombre dentro\n  // del workflow no debe crashear la agregación, y un proveedor best-effort\n  // (CrowdSec) no debe poder marcar el enriquecimiento como parcial ni\n  // contar para el techo de \"fuente única\" — ver el comentario de\n  // CORE_PROVIDERS más arriba.\n  const consultados = (responses ?? []).map((r) => r.provider).filter((p) => CORE_PROVIDERS.includes(p));\n  const ok = CORE_PROVIDERS.filter((p) => enrichment[p].available);\n  const fallidos = consultados.filter((p) => !enrichment[p].available);\n\n  return {\n    ...enrichment,\n    _meta: {\n      providersOk: ok.length,\n      providersFailed: fallidos.length,\n      failed: fallidos,\n      // \"Parcial\" significa: alguno de los proveedores que SÍ se consultó no\n      // respondió. Que no se consulte VirusTotal porque la alerta no tiene\n      // hashes no es una degradación.\n      partial: fallidos.length > 0,\n    },\n  };\n}\n\n/** TTL que corresponde a un veredicto ya parseado. */\nfunction ttlFor(result) {\n  if (!result.available) return CACHE_TTL.error;\n  if (['malicious', 'suspicious'].includes(result.verdict)) return CACHE_TTL.malicious;\n  if (['clean', 'benign'].includes(result.verdict)) return CACHE_TTL.clean;\n  return CACHE_TTL.inconclusive;\n}\n\n\n// ─── fin de lib/enrich.js ───\n\nconst alerta = $input.first().json;\n\nconst ipPrincipal = (alerta.artifacts?.ips ?? []).find((i) => i.direction === 'src')\n  ?? (alerta.artifacts?.ips ?? [])[0];\n\n// Se prefiere SHA256 por ser el que mejor resuelve VirusTotal.\nconst hashes = alerta.artifacts?.hashes ?? [];\nconst hashPrincipal = hashes.find((h) => h.type === 'sha256') ?? hashes[0];\n\nconst consultas = [];\n\nif (ipPrincipal) {\n  consultas.push(\n    { provider: 'abuseipdb', iocType: 'ip', value: ipPrincipal.value, cacheKey: cacheKey('abuseipdb', 'ip', ipPrincipal.value) },\n    { provider: 'virustotal', iocType: 'ip', value: ipPrincipal.value, cacheKey: cacheKey('virustotal', 'ip', ipPrincipal.value) },\n    { provider: 'crowdsec', iocType: 'ip', value: ipPrincipal.value, cacheKey: cacheKey('crowdsec', 'ip', ipPrincipal.value) },\n  );\n}\n\nif (hashPrincipal) {\n  consultas.push(\n    { provider: 'virustotal', iocType: 'hash', value: hashPrincipal.value, cacheKey: cacheKey('virustotal', 'hash', hashPrincipal.value) },\n  );\n}\n\nreturn [\n  {\n    json: {\n      alerta,\n      consultas,\n      enrichStartedAt: Date.now(),\n      ip: ipPrincipal?.value ?? null,\n      hash: hashPrincipal?.value ?? null,\n    },\n  },\n];"
      },
      "id": "wf02-plan",
      "name": "Planificar consultas",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -380,
        300
      ]
    },
    {
      "parameters": {
        "url": "https://api.abuseipdb.com/api/v2/check",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "ipAddress",
              "value": "={{ $json.ip }}"
            },
            {
              "name": "maxAgeInDays",
              "value": "90"
            },
            {
              "name": "verbose",
              "value": ""
            }
          ]
        },
        "options": {
          "timeout": 8000,
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "id": "wf02-abuseipdb",
      "name": "AbuseIPDB v2",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -120,
        250
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "blinksec-abuseipdb",
          "name": "AbuseIPDB API"
        }
      },
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "url": "=https://www.virustotal.com/api/v3/{{ $json.hash ? 'files/' + $json.hash : 'ip_addresses/' + $json.ip }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {
          "timeout": 10000,
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "id": "wf02-virustotal",
      "name": "VirusTotal v3",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -120,
        400
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "blinksec-virustotal",
          "name": "VirusTotal API"
        }
      },
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 20000,
      "onError": "continueRegularOutput",
      "notes": "waitBetweenTries 20 s: el free tier son 4 peticiones/minuto. Reintentar a los 2 s garantiza un segundo 429. El backoff debe ser mayor que la ventana de cuota."
    },
    {
      "parameters": {
        "url": "=https://cti.api.crowdsec.net/v2/smoke/{{ $json.ip }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "options": {
          "timeout": 8000,
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          }
        }
      },
      "id": "wf02-crowdsec",
      "name": "CrowdSec CTI",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -120,
        550
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "blinksec-crowdsec",
          "name": "CrowdSec CTI API"
        }
      },
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 2000,
      "onError": "continueRegularOutput",
      "notes": "Sólo se dispara para IPs — CrowdSec CTI no resuelve hashes, igual que AbuseIPDB."
    },
    {
      "parameters": {
        "numberInputs": 3
      },
      "id": "wf02-merge",
      "name": "Converger respuestas",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 3,
      "position": [
        180,
        325
      ]
    },
    {
      "parameters": {
        "jsCode": "// WF-02 · Agregación de respuestas.\n//\n// Aquí llega TODO, incluidos los proveedores que fallaron: los nodos HTTP\n// tienen neverError y onError=continueRegularOutput precisamente para que un\n// 503 de VirusTotal no aborte la ejecución y deje la alerta sin procesar.\n//\n// El resultado marca explícitamente qué se pudo consultar y qué no. Ese\n// metadato es lo que permite a WF-03 aplicar el techo de puntuación y no\n// contener a ciegas con información incompleta.\n\n// ─── inyectado desde lib/enrich.js — NO EDITAR AQUÍ ───\n// Editar el fichero original y recompilar: npm run build\n/**\n * BlinkSec WF-02 — Enriquecimiento con inteligencia de amenazas.\n *\n * Las llamadas HTTP las hacen los nodos HTTP Request de n8n en paralelo; este\n * módulo contiene lo que sí es testeable en frío: el parseo de cada respuesta\n * a una forma común, la política de caché y el manejo de proveedores caídos.\n *\n * Decisión central: el enriquecimiento es DEGRADABLE. Con proveedores caídos\n * el sistema sigue produciendo un veredicto, pero lo marca como parcial —\n * y WF-03 impone un techo de puntuación para que un enriquecimiento\n * incompleto no pueda disparar contención automática. Es preferible escalar\n * a un humano de más que aislar un servidor porque VirusTotal devolvió 503.\n *\n * Proveedores: AbuseIPDB, VirusTotal y CrowdSec CTI. GreyNoise e IBM X-Force\n * se retiraron del sistema — nunca llegó a haber credenciales operativas\n * para ninguno de los dos (GreyNoise sin clave real conectada; X-Force con\n * un 401 que se confirmó como problema de la propia cuenta de IBM, no de\n * este sistema). CrowdSec CTI las sustituye como tercera fuente para IPs\n * (no resuelve hashes, igual que AbuseIPDB). Ver docs/riesgos.md.\n *\n * @injectable\n */\n\n/** TTL de caché en segundos, por tipo de veredicto. */\nconst CACHE_TTL = {\n  // Un IoC limpio puede ensuciarse: TTL corto.\n  clean: 3600,\n  // Un IoC malicioso rara vez se vuelve benigno: TTL largo, y es el caso que\n  // más consultas repetidas genera (misma botnet atacando en ráfaga).\n  malicious: 86400,\n  // Sin datos suficientes: reintentar pronto.\n  inconclusive: 900,\n  // Error del proveedor: no envenenar la caché con un fallo transitorio.\n  error: 60,\n};\n\nconst PROVIDERS = ['virustotal', 'abuseipdb', 'crowdsec'];\n\n// AbuseIPDB y VirusTotal son las fuentes \"core\": si una de ellas falla, el\n// enriquecimiento se marca parcial y WF-03 aplica el techo de seguridad.\n// CrowdSec es \"best-effort\": con sólo 120 consultas/mes (~4/día) en el plan\n// gratuito, cualquier ráfaga real de alertas agota la cuota en minutos. Si\n// contara como fuente core, un 429 de CrowdSec activaría el techo de\n// enriquecimiento parcial para TODAS las alertas del resto del día, aunque\n// AbuseIPDB y VirusTotal respondan con normalidad — el proveedor más débil\n// rompería la contención automática de todo el sistema. Cuando CrowdSec\n// responde, sigue sumando como corroboración extra en scoring/score.js; si\n// no responde, se ignora sin más, sin degradar nada.\nconst CORE_PROVIDERS = ['virustotal', 'abuseipdb'];\n\nfunction cacheKey(provider, iocType, value) {\n  return `blinksec:ioc:${provider}:${iocType}:${String(value).toLowerCase()}`;\n}\n\n/** Envoltura uniforme para cualquier respuesta de proveedor. */\nfunction wrap(provider, { available, verdict = 'inconclusive', data = {}, error = null }) {\n  return { provider, available, verdict, data, error };\n}\n\n// ---------------------------------------------------------------------------\n// VirusTotal — API v3\n// ---------------------------------------------------------------------------\n\nfunction parseVirusTotal(res, httpStatus = 200) {\n  // 404 = hash/IP desconocido para VT. Ausencia de evidencia, no evidencia de\n  // ausencia: un binario de malware a medida nunca estará en VT.\n  if (httpStatus === 404) {\n    return wrap('virustotal', { available: true, verdict: 'unknown', data: { known: false } });\n  }\n  // 429 = cuota agotada. El free tier son 4 req/min: en una ráfaga de alertas\n  // esto es lo normal, no lo excepcional. Por eso existe la caché.\n  if (httpStatus === 429) {\n    return wrap('virustotal', { available: false, error: 'rate_limited' });\n  }\n  if (httpStatus !== 200 || !res) {\n    return wrap('virustotal', { available: false, error: `HTTP ${httpStatus}` });\n  }\n\n  const stats = res.data?.attributes?.last_analysis_stats ?? {};\n  const malicious = Number(stats.malicious ?? 0);\n  const suspicious = Number(stats.suspicious ?? 0);\n  const harmless = Number(stats.harmless ?? 0);\n  const undetected = Number(stats.undetected ?? 0);\n  const total = malicious + suspicious + harmless + undetected;\n\n  const attrs = res.data?.attributes ?? {};\n  const data = {\n    known: true,\n    malicious,\n    suspicious,\n    total,\n    ratio: total > 0 ? malicious / total : 0,\n    // El nombre de familia que da el consenso, útil para el ticket.\n    popularName: attrs.popular_threat_classification?.suggested_threat_label ?? null,\n    reputation: attrs.reputation ?? null,\n  };\n\n  let verdict = 'inconclusive';\n  if (total === 0) verdict = 'unknown';\n  else if (malicious >= 5) verdict = 'malicious';\n  else if (malicious >= 1) verdict = 'suspicious';\n  else verdict = 'clean';\n\n  return wrap('virustotal', { available: true, verdict, data });\n}\n\n// ---------------------------------------------------------------------------\n// AbuseIPDB — API v2\n// ---------------------------------------------------------------------------\n\nfunction parseAbuseIPDB(res, httpStatus = 200) {\n  if (httpStatus === 429) {\n    return wrap('abuseipdb', { available: false, error: 'rate_limited' });\n  }\n  if (httpStatus !== 200 || !res?.data) {\n    return wrap('abuseipdb', { available: false, error: `HTTP ${httpStatus}` });\n  }\n\n  const d = res.data;\n  const score = Number(d.abuseConfidenceScore ?? 0);\n  const data = {\n    abuseConfidenceScore: score,\n    totalReports: Number(d.totalReports ?? 0),\n    distinctReporters: Number(d.numDistinctUsers ?? 0),\n    countryCode: d.countryCode ?? null,\n    isp: d.isp ?? null,\n    usageType: d.usageType ?? null,\n    lastReportedAt: d.lastReportedAt ?? null,\n    // Una IP dentro de la propia allowlist de AbuseIPDB del usuario.\n    isWhitelisted: d.isWhitelisted === true,\n  };\n\n  let verdict = 'clean';\n  if (d.isWhitelisted === true) verdict = 'benign';\n  else if (score >= 75) verdict = 'malicious';\n  else if (score >= 25) verdict = 'suspicious';\n  else if (score > 0) verdict = 'inconclusive';\n\n  return wrap('abuseipdb', { available: true, verdict, data });\n}\n\n// ---------------------------------------------------------------------------\n// CrowdSec CTI — /v2/smoke/{ip}\n// ---------------------------------------------------------------------------\n\nfunction parseCrowdSec(res, httpStatus = 200) {\n  // 404 = IP nunca reportada a la red de CrowdSec. Ausencia de evidencia, no\n  // evidencia de ausencia — igual que un hash desconocido para VirusTotal.\n  if (httpStatus === 404) {\n    return wrap('crowdsec', { available: true, verdict: 'unknown', data: { known: false } });\n  }\n  if (httpStatus === 429) {\n    return wrap('crowdsec', { available: false, error: 'rate_limited' });\n  }\n  if (httpStatus !== 200 || !res) {\n    return wrap('crowdsec', { available: false, error: `HTTP ${httpStatus}` });\n  }\n\n  // Escala 0-5 de CrowdSec CTI: cuánto de agresiva/amenazante es la IP según\n  // el consenso de la red de sensores CrowdSec.\n  const threat = Number(res.scores?.overall?.threat ?? 0);\n  const aggressiveness = Number(res.scores?.overall?.aggressiveness ?? 0);\n  const clasificaciones = (res.classifications?.classifications ?? []).map((c) => c.label ?? c.name).filter(Boolean);\n\n  const data = {\n    known: true,\n    threat,\n    aggressiveness,\n    classifications: clasificaciones,\n    backgroundNoise: res.background_noise_score != null ? Number(res.background_noise_score) : null,\n  };\n\n  let verdict = 'clean';\n  if (threat >= 3 || clasificaciones.length > 0) verdict = 'malicious';\n  else if (threat >= 1 || aggressiveness >= 1) verdict = 'suspicious';\n\n  return wrap('crowdsec', { available: true, verdict, data });\n}\n\n// ---------------------------------------------------------------------------\n// Agregación\n// ---------------------------------------------------------------------------\n\nconst PARSERS = {\n  virustotal: parseVirusTotal,\n  abuseipdb: parseAbuseIPDB,\n  crowdsec: parseCrowdSec,\n};\n\n/**\n * Consolida las respuestas de los nodos HTTP paralelos.\n *\n * Cada entrada es { provider, body, statusCode } tal y como la deja n8n con\n * \"Continue on Fail\" y \"Never Error\" activados — un proveedor caído llega\n * aquí como un statusCode != 200, no como una excepción que aborte el flujo.\n */\nfunction aggregate(responses) {\n  const enrichment = {};\n  for (const provider of PROVIDERS) enrichment[provider] = wrap(provider, { available: false, error: 'no_consultado' });\n\n  for (const r of responses ?? []) {\n    const parser = PARSERS[r.provider];\n    if (!parser) continue;\n    try {\n      enrichment[r.provider] = parser(r.body, r.statusCode ?? 200);\n    } catch (e) {\n      // Un cambio de esquema del proveedor no puede tumbar el SOAR entero.\n      enrichment[r.provider] = wrap(r.provider, { available: false, error: `parse_error: ${e.message}` });\n    }\n  }\n\n  // Se acota a proveedores CORE conocidos: una errata en el nombre dentro\n  // del workflow no debe crashear la agregación, y un proveedor best-effort\n  // (CrowdSec) no debe poder marcar el enriquecimiento como parcial ni\n  // contar para el techo de \"fuente única\" — ver el comentario de\n  // CORE_PROVIDERS más arriba.\n  const consultados = (responses ?? []).map((r) => r.provider).filter((p) => CORE_PROVIDERS.includes(p));\n  const ok = CORE_PROVIDERS.filter((p) => enrichment[p].available);\n  const fallidos = consultados.filter((p) => !enrichment[p].available);\n\n  return {\n    ...enrichment,\n    _meta: {\n      providersOk: ok.length,\n      providersFailed: fallidos.length,\n      failed: fallidos,\n      // \"Parcial\" significa: alguno de los proveedores que SÍ se consultó no\n      // respondió. Que no se consulte VirusTotal porque la alerta no tiene\n      // hashes no es una degradación.\n      partial: fallidos.length > 0,\n    },\n  };\n}\n\n/** TTL que corresponde a un veredicto ya parseado. */\nfunction ttlFor(result) {\n  if (!result.available) return CACHE_TTL.error;\n  if (['malicious', 'suspicious'].includes(result.verdict)) return CACHE_TTL.malicious;\n  if (['clean', 'benign'].includes(result.verdict)) return CACHE_TTL.clean;\n  return CACHE_TTL.inconclusive;\n}\n\n\n// ─── fin de lib/enrich.js ───\n\nconst plan = $('Planificar consultas').first().json;\nconst alerta = plan.alerta;\n\n// Mapa nodo → proveedor, para reconstruir el origen de cada respuesta.\nconst mapaNodos = [\n  ['AbuseIPDB v2', 'abuseipdb'],\n  ['VirusTotal v3', 'virustotal'],\n  ['CrowdSec CTI', 'crowdsec'],\n];\n\nconst proveedoresPlanificados = new Set(plan.consultas.map((c) => c.provider));\nconst respuestas = [];\n\nfor (const [nombreNodo, provider] of mapaNodos) {\n  if (!proveedoresPlanificados.has(provider)) continue;\n\n  let salida;\n  try {\n    salida = $(nombreNodo).first().json;\n  } catch (e) {\n    // El nodo no llegó a ejecutarse.\n    respuestas.push({ provider, statusCode: 0, body: null });\n    continue;\n  }\n\n  respuestas.push({\n    provider,\n    statusCode: salida.statusCode ?? 200,\n    body: salida.body ?? salida,\n  });\n}\n\nconst enrichment = aggregate(respuestas);\n\nreturn [\n  {\n    json: {\n      ...alerta,\n      enrichment,\n      timings: {\n        ingestStartedAt: alerta.ingestStartedAt,\n        enrichStartedAt: plan.enrichStartedAt,\n        enrichMs: Date.now() - plan.enrichStartedAt,\n      },\n    },\n  },\n];"
      },
      "id": "wf02-aggregate",
      "name": "Agregar inteligencia",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        400,
        325
      ]
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "blinksecwf03tria",
          "mode": "list"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {}
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "wf02-call-triage",
      "name": "Invocar WF-03 Triaje",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        620,
        325
      ]
    }
  ],
  "connections": {
    "Entrada desde WF-01": {
      "main": [
        [
          {
            "node": "Planificar consultas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Planificar consultas": {
      "main": [
        [
          {
            "node": "AbuseIPDB v2",
            "type": "main",
            "index": 0
          },
          {
            "node": "VirusTotal v3",
            "type": "main",
            "index": 0
          },
          {
            "node": "CrowdSec CTI",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "AbuseIPDB v2": {
      "main": [
        [
          {
            "node": "Converger respuestas",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "VirusTotal v3": {
      "main": [
        [
          {
            "node": "Converger respuestas",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "CrowdSec CTI": {
      "main": [
        [
          {
            "node": "Converger respuestas",
            "type": "main",
            "index": 2
          }
        ]
      ]
    },
    "Converger respuestas": {
      "main": [
        [
          {
            "node": "Agregar inteligencia",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Agregar inteligencia": {
      "main": [
        [
          {
            "node": "Invocar WF-03 Triaje",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "errorWorkflow": "blinksecwf99errs"
  },
  "pinData": {},
  "meta": {
    "blinksec": {
      "fase": 3,
      "descripcion": "Consulta en paralelo AbuseIPDB, VirusTotal y CrowdSec CTI. Degrada de forma controlada: con proveedores caídos sigue produciendo veredicto y lo marca como parcial. GreyNoise e IBM X-Force se retiraron del sistema — ver docs/riesgos.md.",
      "tests": "tests/triage/enrich.test.js"
    }
  }
}
